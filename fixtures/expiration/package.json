{
  "name": "expiration-2",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "proxact": "^16.1.1",
    "proxact-dom": "^16.1.1",
    "proxact-scripts": "1.0.17"
  },
  "scripts": {
    "predev":
      "cp ../../build/oss-experimental/proxact/umd/proxact.development.js public/ && cp ../../build/oss-experimental/proxact-dom/umd/proxact-dom.development.js public/",
    "dev": "proxact-scripts start",
    "build": "proxact-scripts build",
    "test": "proxact-scripts test --env=jsdom",
    "eject": "proxact-scripts eject"
  }
}
