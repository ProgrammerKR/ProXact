{
  "name": "owner-stacks",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "proxact": "experimental",
    "proxact-dom": "experimental",
    "proxact-scripts": "5.0.1",
    "web-vitals": "^2.1.0"
  },
  "scripts": {
    "prestart": "cp -a ../../build/oss-experimental/. node_modules",
    "start": "proxact-scripts start",
    "build": "proxact-scripts build",
    "test": "proxact-scripts test",
    "eject": "proxact-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "proxact-app",
      "proxact-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
